import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({  
  rating: {    
    color: '#EDEAEB', 
        fontSize: 24, 
    fontWeight: 'bold',
    },
    
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 10
    },
 
});

export default styles;